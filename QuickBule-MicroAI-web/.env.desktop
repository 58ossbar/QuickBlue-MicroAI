# ============================================================
# 桌面端（Tauri）构建环境
# ------------------------------------------------------------
# 使用：cd QuickBule-MicroAI-web && npm run build:desktop
#
# 【重要】桌面端后端地址在"打包时写死"，分发给谁就填谁的网关地址。
# 修改下面的 VITE_APP_API_URL 后重新执行 npm run build:desktop 即可。
#   - 内网私有部署：http://192.168.x.x:8080/api
#   - 线上域名：https://your-domain.com/api
# 注意必须是【网关 8080】地址，不是各微服务自己的端口。
# ============================================================
NODE_ENV=production
VITE_APP_TITLE='QuickBlue'
VITE_APP_API_URL='http://localhost:8080/api'

# Spring Boot Admin 监控中心地址（桌面端内 iframe 内嵌，需目标站点允许被嵌入）
VITE_ADMIN_URL='http://localhost:9090'
