package com.budaos.common.web.config;

import org.springframework.context.annotation.Configuration;

/**
 * OpenFeign 降级策略配置
 * 在每个 FeignClient 上显式配置 fallback 或 fallbackFactory 使用
 *
 * @author budaos
 */
@Configuration
public class FeignFallbackConfig {
}
