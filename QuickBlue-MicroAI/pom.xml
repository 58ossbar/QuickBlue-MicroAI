<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.budaos</groupId>
    <artifactId>QuickBlue-MicroAI</artifactId>
    <version>4.0.0</version>
    <packaging>pom</packaging>

    <name>QuickBlue MicroAI</name>
    <description>QuickBlue 微服务父项目</description>

    <modules>
        <module>QuickBlue-common</module>
        <module>QuickBlue-api</module>
        <module>QuickBlue-modules</module>
        <module>QuickBlue-gateway</module>
        <module>QuickBlue-admin</module>
    </modules>

    <properties>
        <!-- 项目版本 -->
        <QuickBlue.version>4.0.0</QuickBlue.version>
        <java.version>21</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>

        <!-- Spring Boot 版本（2024-2025 企业级最新稳定版本） -->
        <spring-boot.version>3.5.10</spring-boot.version>
        <spring-cloud.version>2025.0.1</spring-cloud.version>
        <spring-cloud-alibaba.version>2025.0.0.0</spring-cloud-alibaba.version>

        <!-- Nacos 版本 -->
        <nacos.version>2.4.3</nacos.version>

        <!-- 数据库相关 -->
        <mysql-connector.version>8.0.33</mysql-connector.version>
        <druid.version>1.2.25</druid.version>
        <mybatis-plus.version>3.5.7</mybatis-plus.version>
        <mybatis-plus-spring-boot3.version>3.5.7</mybatis-plus-spring-boot3.version>
        <p6spy.version>3.9.1</p6spy.version>

        <!-- Redis 相关 -->
        <redisson.version>3.50.0</redisson.version>

        <!-- 本地缓存 -->
        <caffeine.version>3.1.8</caffeine.version>

        <!-- 权限认证 -->
        <sa-token.version>1.44.0</sa-token.version>
        <sa-token-core.version>1.44.0</sa-token-core.version>

        <!-- 工具库 -->
        <hutool.version>5.8.39</hutool.version>
        <guava.version>33.3.1-jre</guava.version>
        <fastjson2.version>2.0.57</fastjson2.version>
        <commons-io.version>2.18.0</commons-io.version>
        <commons-lang3.version>3.17.0</commons-lang3.version>
        <commons-collections4.version>4.4</commons-collections4.version>
        <commons-codec.version>1.17.1</commons-codec.version>
        <commons-compress.version>1.27.1</commons-compress.version>
        <commons-text.version>1.13.0</commons-text.version>

        <!-- Excel 处理 -->
        <easyexcel.version>4.0.3</easyexcel.version>
        <poi.version>5.4.1</poi.version>

        <!-- 模板引擎 -->
        <velocity.version>2.4.1</velocity.version>
        <velocity-tools.version>3.1</velocity-tools.version>
        <freemarker.version>2.3.34</freemarker.version>
        <!-- HTML 解析 -->
        <jsoup.version>1.21.1</jsoup.version>

        <!-- 其他功能 -->
        <zxing.version>3.5.3</zxing.version>
        <tinypinyin.version>2.0.3</tinypinyin.version>
        <ip2region.version>2.7.0</ip2region.version>
        <bouncycastle.version>1.80</bouncycastle.version>
        <resilience4j.version>2.1.0</resilience4j.version>
        <aws-java-sdk-s3.version>2.31.78</aws-java-sdk-s3.version>

        <!-- 消息队列 -->
        <rocketmq-spring-boot.version>2.3.1</rocketmq-spring-boot.version>

        <!-- 分布式事务 -->
        <seata.version>2.0.0</seata.version>

        <!-- 熔断降级 -->
        <sentinel.version>1.8.8</sentinel.version>

        <!-- 链路追踪 -->
        <spring-cloud-starter-sleuth.version>3.1.12</spring-cloud-starter-sleuth.version>

        <!-- OpenFeign -->
        <okhttp.version>4.12.0</okhttp.version>
        <feign-okhttp.version>13.1</feign-okhttp.version>

        <!-- API 文档 -->
        <!-- Knife4j 4.6.0 稳定版本，兼容 SpringDoc 2.8.6 (Spring Boot 3.5.x) -->
        <knife4j.version>4.6.0</knife4j.version>
        <springdoc.version>2.8.6</springdoc.version>
        <swagger.version>2.2.30</swagger.version>

        <!-- 监控管理 -->
        <spring-boot-admin.version>3.4.4</spring-boot-admin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot Dependencies -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring Cloud Dependencies -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring Cloud Alibaba Dependencies -->
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring-cloud-alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- ==================== 项目内部模块版本管理 ==================== -->

            <!-- Common 模块 -->
            <dependency>
                <groupId>com.budaos</groupId>
                <artifactId>QuickBlue-common-core</artifactId>
                <version>${QuickBlue.version}</version>
            </dependency>
            <dependency>
                <groupId>com.budaos</groupId>
                <artifactId>QuickBlue-common-database</artifactId>
                <version>${QuickBlue.version}</version>
            </dependency>
            <dependency>
                <groupId>com.budaos</groupId>
                <artifactId>QuickBlue-common-redis</artifactId>
                <version>${QuickBlue.version}</version>
            </dependency>
            <dependency>
                <groupId>com.budaos</groupId>
                <artifactId>QuickBlue-common-security</artifactId>
                <version>${QuickBlue.version}</version>
            </dependency>
            <dependency>
                <groupId>com.budaos</groupId>
                <artifactId>QuickBlue-common-swagger</artifactId>
                <version>${QuickBlue.version}</version>
            </dependency>
            <dependency>
                <groupId>com.budaos</groupId>
                <artifactId>QuickBlue-common-web</artifactId>
                <version>${QuickBlue.version}</version>
            </dependency>
            <dependency>
                <groupId>com.budaos</groupId>
                <artifactId>QuickBlue-common-log</artifactId>
                <version>${QuickBlue.version}</version>
            </dependency>
            <dependency>
                <groupId>com.budaos</groupId>
                <artifactId>QuickBlue-common-excel</artifactId>
                <version>${QuickBlue.version}</version>
            </dependency>

            <!-- API 模块 -->
            <dependency>
                <groupId>com.budaos</groupId>
                <artifactId>QuickBlue-api-system</artifactId>
                <version>${QuickBlue.version}</version>
            </dependency>
            <dependency>
                <groupId>com.budaos</groupId>
                <artifactId>QuickBlue-api-business</artifactId>
                <version>${QuickBlue.version}</version>
            </dependency>
            <dependency>
                <groupId>com.budaos</groupId>
                <artifactId>QuickBlue-api-support</artifactId>
                <version>${QuickBlue.version}</version>
            </dependency>
            <dependency>
                <groupId>com.budaos</groupId>
                <artifactId>QuickBlue-api-ai</artifactId>
                <version>${QuickBlue.version}</version>
            </dependency>

            <!-- Modules 模块 -->
            <dependency>
                <groupId>com.budaos</groupId>
                <artifactId>QuickBlue-system</artifactId>
                <version>${QuickBlue.version}</version>
            </dependency>
            <dependency>
                <groupId>com.budaos</groupId>
                <artifactId>QuickBlue-business</artifactId>
                <version>${QuickBlue.version}</version>
            </dependency>
            <dependency>
                <groupId>com.budaos</groupId>
                <artifactId>QuickBlue-support</artifactId>
                <version>${QuickBlue.version}</version>
            </dependency>
            <dependency>
                <groupId>com.budaos</groupId>
                <artifactId>QuickBlue-ai</artifactId>
                <version>${QuickBlue.version}</version>
            </dependency>
            <dependency>
                <groupId>com.budaos</groupId>
                <artifactId>QuickBlue-admin</artifactId>
                <version>${QuickBlue.version}</version>
            </dependency>

            <!-- ==================== 第三方依赖版本管理 ==================== -->

            <!-- Nacos -->
            <dependency>
                <groupId>com.alibaba.nacos</groupId>
                <artifactId>nacos-client</artifactId>
                <version>${nacos.version}</version>
            </dependency>

            <!-- 数据库相关 -->
            <dependency>
                <groupId>mysql</groupId>
                <artifactId>mysql-connector-java</artifactId>
                <version>${mysql-connector.version}</version>
            </dependency>
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>druid-spring-boot-starter</artifactId>
                <version>${druid.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus-spring-boot3.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-extension</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>p6spy</groupId>
                <artifactId>p6spy</artifactId>
                <version>${p6spy.version}</version>
            </dependency>

            <!-- Redis 相关 -->
            <dependency>
                <groupId>org.redisson</groupId>
                <artifactId>redisson-spring-boot-starter</artifactId>
                <version>${redisson.version}</version>
            </dependency>

            <!-- 权限认证 -->
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-spring-boot3-starter</artifactId>
                <version>${sa-token.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-core</artifactId>
                <version>${sa-token.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-redis-jackson</artifactId>
                <version>${sa-token.version}</version>
            </dependency>

            <!-- 工具库 -->
            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-all</artifactId>
                <version>${hutool.version}</version>
            </dependency>
            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>${guava.version}</version>
            </dependency>
            <dependency>
                <groupId>com.alibaba.fastjson2</groupId>
                <artifactId>fastjson2</artifactId>
                <version>${fastjson2.version}</version>
            </dependency>
            <dependency>
                <groupId>commons-io</groupId>
                <artifactId>commons-io</artifactId>
                <version>${commons-io.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${commons-lang3.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-collections4</artifactId>
                <version>${commons-collections4.version}</version>
            </dependency>
            <dependency>
                <groupId>commons-codec</groupId>
                <artifactId>commons-codec</artifactId>
                <version>${commons-codec.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-compress</artifactId>
                <version>${commons-compress.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-text</artifactId>
                <version>${commons-text.version}</version>
            </dependency>

            <!-- Excel 处理 -->
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>easyexcel</artifactId>
                <version>${easyexcel.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.poi</groupId>
                <artifactId>poi</artifactId>
                <version>${poi.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.poi</groupId>
                <artifactId>poi-ooxml</artifactId>
                <version>${poi.version}</version>
            </dependency>

            <!-- 模板引擎 -->
            <dependency>
                <groupId>org.apache.velocity</groupId>
                <artifactId>velocity-engine-core</artifactId>
                <version>${velocity.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.velocity.tools</groupId>
                <artifactId>velocity-tools-generic</artifactId>
                <version>${velocity-tools.version}</version>
            </dependency>
            <!--velocity end-->
            <dependency>
                <groupId>org.freemarker</groupId>
                <artifactId>freemarker</artifactId>
                <version>${freemarker.version}</version>
            </dependency>

            <!-- HTML 解析 -->
            <dependency>
                <groupId>org.jsoup</groupId>
                <artifactId>jsoup</artifactId>
                <version>${jsoup.version}</version>
            </dependency>

            <!-- 其他功能 -->
            <dependency>
                <groupId>com.google.zxing</groupId>
                <artifactId>core</artifactId>
                <version>${zxing.version}</version>
            </dependency>
            <dependency>
                <groupId>com.google.zxing</groupId>
                <artifactId>javase</artifactId>
                <version>${zxing.version}</version>
            </dependency>
            <dependency>
                <groupId>io.github.biezhi</groupId>
                <artifactId>TinyPinyin</artifactId>
                <version>${tinypinyin.version}</version>
            </dependency>
            <dependency>
                <groupId>org.lionsoul</groupId>
                <artifactId>ip2region</artifactId>
                <version>${ip2region.version}</version>
            </dependency>
            <dependency>
                <groupId>org.bouncycastle</groupId>
                <artifactId>bcprov-jdk18on</artifactId>
                <version>${bouncycastle.version}</version>
            </dependency>
            <dependency>
                <groupId>io.github.resilience4j</groupId>
                <artifactId>resilience4j-spring-boot3</artifactId>
                <version>${resilience4j.version}</version>
            </dependency>
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>s3</artifactId>
                <version>${aws-java-sdk-s3.version}</version>
            </dependency>

            <!-- 消息队列 -->
            <dependency>
                <groupId>org.apache.rocketmq</groupId>
                <artifactId>rocketmq-spring-boot-starter</artifactId>
                <version>${rocketmq-spring-boot.version}</version>
            </dependency>

            <!-- 分布式事务 -->
            <dependency>
                <groupId>io.seata</groupId>
                <artifactId>seata-spring-boot-starter</artifactId>
                <version>${seata.version}</version>
            </dependency>

            <!-- 熔断降级 -->
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-starter-alibaba-sentinel</artifactId>
                <version>${spring-cloud-alibaba.version}</version>
            </dependency>

            <!-- 链路追踪 -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-starter-sleuth</artifactId>
                <version>${spring-cloud-starter-sleuth.version}</version>
            </dependency>

            <!-- 本地缓存 -->
            <dependency>
                <groupId>com.github.ben-manes.caffeine</groupId>
                <artifactId>caffeine</artifactId>
                <version>${caffeine.version}</version>
            </dependency>

            <!-- API 文档 -->
            <dependency>
                <groupId>com.github.xingfudeshi</groupId>
                <artifactId>knife4j-core</artifactId>
                <version>${knife4j.version}</version>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/com.github.xingfudeshi/knife4j-openapi3-ui -->
            <dependency>
                <groupId>com.github.xingfudeshi</groupId>
                <artifactId>knife4j-openapi3-ui</artifactId>
                <version>${knife4j.version}</version>
                <scope>compile</scope>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/com.github.xingfudeshi/knife4j-openapi3-jakarta-spring-boot-starter -->
            <dependency>
                <groupId>com.github.xingfudeshi</groupId>
                <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
                <version>${knife4j.version}</version>
                <scope>compile</scope>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-common</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-api</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webflux-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <!-- Swagger 依赖版本管理 -->
            <dependency>
                <groupId>io.swagger.core.v3</groupId>
                <artifactId>swagger-core-jakarta</artifactId>
                <version>${swagger.version}</version>
            </dependency>
            <dependency>
                <groupId>io.swagger.core.v3</groupId>
                <artifactId>swagger-annotations-jakarta</artifactId>
                <version>${swagger.version}</version>
            </dependency>
            <dependency>
                <groupId>io.swagger.core.v3</groupId>
                <artifactId>swagger-models-jakarta</artifactId>
                <version>${swagger.version}</version>
            </dependency>

            <!-- 监控管理 - Spring Boot Admin -->
            <dependency>
                <groupId>de.codecentric</groupId>
                <artifactId>spring-boot-admin-starter-server</artifactId>
                <version>${spring-boot-admin.version}</version>
            </dependency>
            <dependency>
                <groupId>de.codecentric</groupId>
                <artifactId>spring-boot-admin-starter-client</artifactId>
                <version>${spring-boot-admin.version}</version>
            </dependency>

            <!-- OpenFeign OkHttp -->
            <dependency>
                <groupId>io.github.openfeign</groupId>
                <artifactId>feign-okhttp</artifactId>
                <version>${feign-okhttp.version}</version>
            </dependency>
            <dependency>
                <groupId>com.squareup.okhttp3</groupId>
                <artifactId>okhttp</artifactId>
                <version>${okhttp.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot.version}</version>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <compilerArgs>
                        <arg>-parameters</arg>
                    </compilerArgs>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>1.18.34</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <!-- 阿里云公共仓库 (主仓库) -->
        <repository>
            <id>aliyunmaven</id>
            <name>阿里云公共仓库</name>
            <url>https://maven.aliyun.com/repository/public</url>
        </repository>
        
        <!-- Maven Central (备用仓库，用于阿里云没有的依赖) -->
        <repository>
            <id>central</id>
            <name>Maven Central</name>
            <url>https://repo1.maven.org/maven2/</url>
        </repository>
        
        <!-- Spring Milestones (Spring 里程碑版本) -->
        <repository>
            <id>spring-milestones</id>
            <name>Spring Milestones</name>
            <url>https://repo.spring.io/milestone</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>
</project>
