<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.budaos.system.dao.AuthRoleMenuDao">

    <delete id="deleteByRoleId">
        DELETE
        FROM t_role_menu
        WHERE role_id = #{roleId}
    </delete>

    <select id="queryMenuIdByRoleId" resultType="java.lang.Long">
        SELECT menu_id
        FROM t_role_menu
        WHERE role_id = #{roleId}
    </select>

    <select id="queryAllRoleMenu" resultType="com.budaos.system.domain.entity.AuthRoleMenuEntity">
        SELECT *
        FROM t_role_menu
    </select>

    <select id="selectMenuListByRoleIdList" resultType="com.budaos.system.domain.entity.NavMenuEntity">
        SELECT DISTINCT t_menu.*
        FROM t_menu
                 LEFT JOIN t_role_menu ON t_role_menu.menu_id = t_menu.menu_id
        <where>
            <if test="deletedFlag != null">
                AND t_menu.deleted_flag = #{deletedFlag}
            </if>
            <if test="roleIdList != null and roleIdList.size > 0">
                AND t_role_menu.role_id IN
                <foreach collection="roleIdList" open="(" close=")" item="item" separator=",">
                    #{item}
                </foreach>
            </if>
        </where>
        ORDER BY t_menu.sort ASC
    </select>

</mapper>
