<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.budaos.system.dao.AuditLogDao">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.budaos.system.domain.entity.AuditLogEntity">
        <id column="operate_log_id" property="operateLogId" />
        <result column="operate_user_id" property="operateUserId" />
        <result column="operate_user_type" property="operateUserType" />
        <result column="operate_user_name" property="operateUserName" />
        <result column="module" property="module" />
        <result column="content" property="content" />
        <result column="url" property="url" />
        <result column="method" property="method" />
        <result column="param" property="param" />
        <result column="response" property="response" />
        <result column="ip" property="ip" />
        <result column="ip_region" property="ipRegion" />
        <result column="user_agent" property="userAgent" />
        <result column="success_flag" property="successFlag" />
        <result column="fail_reason" property="failReason" />
        <result column="update_time" property="updateTime" />
        <result column="create_time" property="createTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        operate_log_id, operate_user_id, operate_user_type, operate_user_name, module, content, url, method, param, response, ip, ip_region, user_agent, success_flag, fail_reason, update_time, create_time
    </sql>

    <!-- 分页查询 -->
    <select id="queryByPage" resultType="com.budaos.system.domain.entity.AuditLogEntity">
        SELECT
        <include refid="Base_Column_List" />
        FROM t_operate_log
        <where>
            <if test="query.operateUserId != null">
                AND operate_user_id = #{query.operateUserId}
            </if>
            <if test="query.operateUserType != null">
                AND operate_user_type = #{query.operateUserType}
            </if>
            <if test="query.startDate != null and query.startDate != ''">
                AND DATE_FORMAT(create_time, '%Y-%m-%d') &gt;= #{query.startDate}
            </if>
            <if test="query.endDate != null and query.endDate != ''">
                AND DATE_FORMAT(create_time, '%Y-%m-%d') &lt;= #{query.endDate}
            </if>
            <if test="query.userName != null and query.userName != ''">
                AND INSTR(operate_user_name, #{query.userName})
            </if>
            <if test="query.keywords != null and query.keywords != ''">
                AND (INSTR(module, #{query.keywords}) OR INSTR(content, #{query.keywords}))
            </if>
            <if test="query.requestKeywords != null and query.requestKeywords != ''">
                AND (INSTR(url, #{query.requestKeywords}) OR INSTR(method, #{query.requestKeywords}) OR INSTR(param, #{query.requestKeywords}) OR INSTR(response, #{query.requestKeywords}))
            </if>
            <if test="query.successFlag != null">
                AND success_flag = #{query.successFlag}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <!-- 批量删除 -->
    <delete id="batchDelete">
        DELETE FROM t_operate_log
        WHERE operate_log_id IN
        <foreach collection="idList" open="(" close=")" separator="," item="item">
            #{item}
        </foreach>
    </delete>

</mapper>
