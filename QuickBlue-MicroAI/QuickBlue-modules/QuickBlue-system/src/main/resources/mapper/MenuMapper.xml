<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.budaos.system.dao.NavMenuDao">

    <update id="deleteByMenuIdList">
        <if test="menuIdList != null and menuIdList.size > 0">
            UPDATE t_menu
            SET deleted_flag = #{deletedFlag},
                update_user_id = #{updateUserId},
                update_time = NOW()
            WHERE menu_id IN
            <foreach collection="menuIdList" item="item" open="(" close=")" separator=",">
                #{item}
            </foreach>
        </if>
    </update>

    <select id="getByMenuName"
            resultType="com.budaos.system.domain.entity.NavMenuEntity">
        SELECT *
        FROM t_menu
        WHERE menu_name = #{menuName}
          AND parent_id = #{parentId}
          AND deleted_flag = #{deletedFlag}
        LIMIT 1
    </select>

    <select id="getByWebPerms"
            resultType="com.budaos.system.domain.entity.NavMenuEntity">
        SELECT *
        FROM t_menu
        WHERE web_perms = #{webPerms}
          AND deleted_flag = #{deletedFlag}
        LIMIT 1
    </select>

    <select id="queryMenuList" resultType="com.budaos.system.domain.vo.NavMenuVO">
        SELECT *
        FROM t_menu
        WHERE deleted_flag = #{deletedFlag}
        <if test="disabledFlag != null">
            AND disabled_flag = #{disabledFlag}
        </if>
        <if test="menuTypeList != null and menuTypeList.size > 0">
            AND menu_type IN
            <foreach collection="menuTypeList" open="(" close=")" item="item" separator=",">
                #{item}
            </foreach>
        </if>
        ORDER BY parent_id ASC, sort ASC
    </select>

    <select id="getPointListByMenuId"
            resultType="com.budaos.system.domain.entity.NavMenuEntity">
        SELECT *
        FROM t_menu
        WHERE parent_id = #{menuId}
          AND deleted_flag = #{deletedFlag}
          AND menu_type = #{menuType}
    </select>

    <select id="queryMenuByEmployeeId"
            resultType="com.budaos.system.domain.vo.NavMenuVO">
        SELECT t_menu.*
        FROM t_menu
                 LEFT JOIN t_role_menu ON t_role_menu.menu_id = t_menu.menu_id
                 LEFT JOIN t_role_employee ON t_role_employee.role_id = t_role_menu.role_id
        WHERE t_menu.disabled_flag = #{disabledFlag}
          AND t_menu.deleted_flag = #{deletedFlag}
        <if test="employeeId != null">
            AND t_role_employee.employee_id = #{employeeId}
        </if>
    </select>

    <select id="queryMenuByType"
            resultType="com.budaos.system.domain.entity.NavMenuEntity">
        SELECT *
        FROM t_menu
        WHERE disabled_flag = #{disabledFlag}
          AND deleted_flag = #{deletedFlag}
          AND menu_type = #{menuType}
    </select>

    <select id="selectMenuIdByParentIdList" resultType="java.lang.Long">
        <if test="menuIdList != null and menuIdList.size > 0">
            SELECT menu_id
            FROM t_menu
            WHERE parent_id IN
            <foreach collection="menuIdList" open="(" close=")" item="item" separator=",">
                #{item}
            </foreach>
        </if>
    </select>

</mapper>
